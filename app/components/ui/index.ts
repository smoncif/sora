/**
 * Composants UI
 * 
 * Ce dossier contient des composants UI réutilisables utilisés dans l'ensemble de l'application.
 * Les composants dans ce dossier doivent être génériques et non liés à une logique métier spécifique.
 */

// Exporter les composants UI ici au fur et à mesure qu'ils sont créés
// Exemple: export { default as Button } from './Button'; 

